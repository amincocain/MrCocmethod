#!/bin/bash

# Configuration Paths
CONF_FILE="/etc/gre_tunnel_config.sh"
MONITOR_FILE="/usr/local/bin/gre_monitor.sh"
VARS_FILE="/etc/gre_vars.conf"
TG_CONF="/etc/gre_tg.conf"

show_menu() {
    echo -e "\n\e[1;34m--- GRE Tunnel Manager (MTU Enabled) ---\e[0m"
    echo "1) Setup Tunnel & Port Forwarding"
    echo "2) Telegram Notification Settings"
    echo "3) Enable Persistence & Monitoring"
    echo "4) Change MTU of existing Tunnels"
    echo "5) Show Status (Tunnels, Ports & MTU)"
    echo "6) Uninstall Everything"
    echo "7) Exit"
    echo -e "\e[1;33m----------------------------------------\e[0m"
    read -p "Please select an option [1-7]: " choice
}

setup_tunnel() {
    read -p "Tunnel Number (1 or 2): " num
    read -p "Local IP (Iran Server): " localip
    read -p "Remote IP (Destination Server): " remoteip
    read -p "Internal Tunnel IP (Default: 10.0.$num.1): " tunnelip
    read -p "Ports to forward (comma separated, e.g. 80,443): " ports
    
    # MTU handling
    default_mtu=1436
    echo -e "\e[1;35mDefault MTU is $default_mtu\e[0m"
    read -p "Enter MTU value (Press Enter for default): " usermtu
    mtu=${usermtu:-$default_mtu}

    # Create GRE Interface
    ip tunnel add gre$num mode gre remote $remoteip local $localip ttl 255
    ip link set gre$num up
    ip addr add $tunnelip/30 dev gre$num
    ip link set dev gre$num mtu $mtu
    
    sysctl -w net.ipv4.ip_forward=1 > /dev/null

    IFS=',' read -ra ADDR <<< "$ports"
    for port in "${ADDR[@]}"; do
        iptables -t nat -A PREROUTING -p tcp --dport $port -j DNAT --to-destination $tunnelip
        iptables -t nat -A PREROUTING -p udp --dport $port -j DNAT --to-destination $tunnelip
    done
    iptables -t nat -A POSTROUTING -d $tunnelip -j MASQUERADE

    # Save commands
    echo "ip tunnel add gre$num mode gre remote $remoteip local $localip ttl 255" >> $CONF_FILE
    echo "ip link set gre$num up" >> $CONF_FILE
    echo "ip addr add $tunnelip/30 dev gre$num" >> $CONF_FILE
    echo "ip link set dev gre$num mtu $mtu" >> $CONF_FILE
    echo "REMOTE_IP_$num=$remoteip" >> $VARS_FILE
    
    echo -e "\e[1;32m✅ Tunnel $num created with MTU $mtu successfully!\e[0m"
}

change_mtu() {
    read -p "Enter Tunnel Name (gre1 or gre2): " tname
    if ip link show $tname > /dev/null 2>&1; then
        current_mtu=$(ip link show $tname | awk '{print $5}')
        echo -e "\e[1;36mCurrent MTU for $tname is: $current_mtu\e[0m"
        read -p "Enter NEW MTU value: " newmtu
        ip link set dev $tname mtu $newmtu
        
        # Update config file for persistence
        sed -i "s/ip link set dev $tname mtu.*/ip link set dev $tname mtu $newmtu/g" $CONF_FILE
        
        echo -e "\e[1;32m✅ MTU updated to $newmtu for $tname.\e[0m"
    else
        echo -e "\e[1;31m❌ Tunnel $tname not found!\e[0m"
    fi
}

setup_telegram() {
    read -p "Bot Token: " token
    read -p "Chat ID: " chatid
    echo "TG_TOKEN=\"$token\"" > $TG_CONF
    echo "TG_CHATID=\"$chatid\"" >> $TG_CONF
    echo -e "\e[1;32m✅ Telegram settings saved.\e[0m"
}

make_persistent() {
    echo "Applying system services..."
    cat <<'EOF' > $MONITOR_FILE
#!/bin/bash
[ -f /etc/gre_tg.conf ] && source /etc/gre_tg.conf
[ -f /etc/gre_vars.conf ] && source /etc/gre_vars.conf
check_and_notify() {
    IP=$1; NAME=$2
    ping -c 3 $IP > /dev/null 2>&1
    if [ $? -ne 0 ]; then
        MESSAGE="⚠️ ALERT! Connection to $NAME ($IP) is DOWN!"
        curl -s -X POST "https://api.telegram.org/bot$TG_TOKEN/sendMessage" -d "chat_id=$TG_CHATID&text=$MESSAGE"
    fi
}
[ ! -z "$REMOTE_IP_1" ] && check_and_notify "$REMOTE_IP_1" "Remote Server 1"
[ ! -z "$REMOTE_IP_2" ] && check_and_notify "$REMOTE_IP_2" "Remote Server 2"
EOF
    chmod +x $MONITOR_FILE
    echo "#!/bin/bash" > /usr/local/bin/gre_startup.sh
    echo "sysctl -w net.ipv4.ip_forward=1" >> /usr/local/bin/gre_startup.sh
    [ -f $CONF_FILE ] && cat $CONF_FILE >> /usr/local/bin/gre_startup.sh
    echo "iptables -t mangle -A FORWARD -p tcp --tcp-flags SYN,RST SYN -j TCPMSS --clamp-mss-to-pmtu" >> /usr/local/bin/gre_startup.sh
    chmod +x /usr/local/bin/gre_startup.sh
    (crontab -l 2>/dev/null | grep -v "$MONITOR_FILE" ; echo "*/5 * * * * $MONITOR_FILE") | crontab -
    echo -e "\e[1;32m✅ Persistence and Monitoring enabled.\e[0m"
}

uninstall() {
    crontab -l 2>/dev/null | grep -v "$MONITOR_FILE" | crontab - 2>/dev/null
    rm $TG_CONF $VARS_FILE $CONF_FILE $MONITOR_FILE /usr/local/bin/gre_startup.sh 2>/dev/null
    ip link set gre1 down 2>/dev/null; ip tunnel del gre1 2>/dev/null
    ip link set gre2 down 2>/dev/null; ip tunnel del gre2 2>/dev/null
    iptables -F -t nat
    echo -e "\e[1;31m✅ All settings removed.\e[0m"
}

while true; do
    show_menu
    case $choice in
        1) setup_tunnel ;;
        2) setup_telegram ;;
        3) make_persistent ;;
        4) change_mtu ;;
        5) 
            echo -e "\n--- Tunnel Status & MTU ---"
            ip -d link show type gre | grep -E 'mtu|gre'
            echo "--- Port Rules (NAT) ---"
            iptables -t nat -L PREROUTING -n -v
            ;;
        6) uninstall ;;
        7) exit 0 ;;
    esac
done
